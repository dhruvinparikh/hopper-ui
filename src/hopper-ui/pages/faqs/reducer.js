import { faqs } from '../../../constants/faqs';

const initialState = {
  faqs
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
  default:
    return {
      ...state
    };
  }
};

export default reducer;