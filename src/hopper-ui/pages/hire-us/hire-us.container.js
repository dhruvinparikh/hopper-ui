import { connect } from 'react-redux';
import HireUS from './hire-us.component';

const mapStateToProps = () => {
  return {};
};

const mapDispatchToProps = null;

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(HireUS);
