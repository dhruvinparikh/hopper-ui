import { connect } from 'react-redux';
import Root from './root';

const mapStateToProps = () => {
  return {};
};

const maapDispatchToProps = null;

export default connect(
  mapStateToProps,
  maapDispatchToProps
)(Root);
